// A function is a block of code that does a task and can be reused.

function add(a: number, b: number): number {
  return a + b;
}

add(2, 3); // 5

// a: number, b: number → parameter types

// : number → return type

// TypeScript checks types before running the code


// Function without return
// function greet(name: string): void {
//   console.log("Hello " + name);
// }

// greet("Novac");

// Arrow Function Example
// const multiply = (x: number, y: number): number => x * y;

// multiply(2, 4); // 8

// Simple explanation

// A function takes input, performs an action, and may return a result.

// One-line interview answer

// Functions in TypeScript can have typed parameters and return values to ensure type safety.