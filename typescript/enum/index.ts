// An enum is used to define a set of named constant values.
// It helps make code more readable and less error-prone.

enum Direction {
  Up,
  Down,
  Left,
  Right
}

let move: Direction = Direction.Up;
console.log(move); // 0


// enum Status {
//   Success = "SUCCESS",
//   Failed = "FAILED"
// }

// let result: Status = Status.Success; // ✅


// One-line interview answer

// Enums are used to define a fixed set of named constants in TypeScript.